let CustomErrorHandler,JwtService;_b50‍.x([["default",()=>_b50‍.o]]);_b50‍.w("../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_b50‍.w("../services/JwtService",[["default",["JwtService"],function(v){JwtService=v}]]);


const auth = async (req,res,next)=>{

    let authHeader = req.headers.authorization;
    _b50‍.g.console.log(authHeader);

    if(!authHeader){
        console.log("------- header h nahi hai -----");
        return next(CustomErrorHandler.unauthorized());
    }

    const token = authHeader.split(' ')[1];
    try {
        const {_id,role}= await JwtService.verify(token);
        const user = {
            _id,
            role
        }
        req.user=user;
        next();

    } catch (error) {
        console.log("------token not varified----");
        return next(CustomErrorHandler.unauthorized());
    }

}

_b50‍.d(auth);